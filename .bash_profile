# Bash Configuration (for occasional bash sessions)

# Shared environment (Homebrew, NVM, Ruby, paths)
source ~/.config/shell/env.sh

# Source .bashrc if it exists (for interactive shells)
[ -f ~/.bashrc ] && source ~/.bashrc
